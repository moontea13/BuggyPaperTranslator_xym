做的时候修复的一些未声明的坑：
    - `import request` -> 拼写错误 requests; `import qtmd` -> 拼写错误 tqdm显示进度
    - 每次翻译后暂停1000秒，目的是为了避免触发翻译API的速率限制，但是时间过长，故改成2秒。
    - 翻译写入函数中需要加入表头。
    - 表头并没有id的元素，故改为title来区分识别每一个文章。同时原代码只翻译了abstract没有翻译title，也需要翻译。翻译时进行断点续传的处理，如果有问题则continue。
    - 文件 result.csv 在每次循环中都以写入模式（"w"）打开，每次迭代都会覆盖之前的内容。应该改为追加模式（"a"）。
    - 大模型翻译的时候会加空行影响输出格式样式，在prompt中加命令即可解决

1. **环境修复**

2. **代码重构**：
   - **API 升级**：使用的是`OpenAI` 兼容接口
   - **模型修正**：模型的参数若干错误的修复说明:
     - `temperature`为文本创造性，参数设置在0-0.3适合学术论文翻译
     - `max_tokens`为输出长度，1个token ≈ 0.75个单词 ≈ 1-2个汉字，需要留出一定空间
   - **Prompt 优化**：简单优化了一下，声明了角色和一些要求
   - **异常处理**：增加 `try-except` 和 `tenacity`（或手动）重试机制，防止因网络波动或 Rate Limit (429) 导致程序崩溃。
   - **Token 安全**：已经改为从环境变量读取。

3. **工程优化 (Bonus)**：
   - **进度显示**：引入 `tqdm` 显示进度。
   - **断点续传**：在循环中用continue解决